# ── Loan portal server config — template ──────────────────────────────────────
# Copy this file to serve_config.py and fill in your values.
# serve_config.py is gitignored — never commit it.
#
# ORCHESTRATOR_BASE : https://cloud.uipath.com/{org}/{tenant}/orchestrator_
# BUCKET_ID         : Orchestrator → Storage Buckets → click bucket → URL shows /Buckets(XXXXX)
# FOLDER_ID         : Orchestrator → your folder → URL shows fid=XXXXX

ORCHESTRATOR_BASE = "https://cloud.uipath.com/YOUR_ORG/YOUR_TENANT/orchestrator_"
BUCKET_ID         = "YOUR_BUCKET_ID"
FOLDER_ID         = "YOUR_FOLDER_ID"
